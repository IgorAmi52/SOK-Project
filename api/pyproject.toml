[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "graph-api"
version = "0.1.0"
description = "Shared graph domain model and plugin contracts"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
