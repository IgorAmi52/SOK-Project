[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "data-source-yaml"
version = "0.1.0"
description = "YAML data source plugin for graph visualization platform"
requires-python = ">=3.11"
dependencies = [
    "graph-api",
    "PyYAML",
]

[tool.setuptools]
packages = ["yaml_data_source"]
